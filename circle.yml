# circle.yml

general:
  artifacts:
    - "target/scala-2.11/coverage-report"

dependencies:
  override:
    - "sbt clean coverage"

test:
  override:
    - "sbt test"
  post:
    - "sbt coveralls"
